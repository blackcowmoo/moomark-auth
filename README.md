# moomark-auth

## Environments

- **Database**: MySQL (production)
- **Test Database**: No dedicated test database - tests use mocking instead of H2
- **Note**: H2 database dependency was removed as tests use mocking instead of in-memory database
