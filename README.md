# moomark-auth

## Environments

## Configuration
The application uses `application.yaml` for Spring Boot configuration (OAuth2, database, JPA) and custom properties (passport keys, default user picture). This configuration file is required regardless of whether the application code is written in Java or Kotlin.
